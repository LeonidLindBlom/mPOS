package controlsys.UpdateVerifierInterface

typedef UInt8 Bool;

const UInt8 VersionLen = 16;

interface {
    Verify(in string<VersionLen> version,
           in UInt32 totalSize,
           in Bool signatureOk,
           out Bool ok);
}
